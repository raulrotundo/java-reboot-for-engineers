package com.javareboot.patterns;

// Factory Pattern: creates objects based on input type.
// Promotes loose coupling by abstracting object creation.
// Implementation: The factory method returns different implementations based on input.
public interface Animal { String speak(); }
